sudo: required
dist: precise

os:
  - linux
  - osx
  - windows

language: go
go:
  - tip

matrix:
  include:
    - os: osx
      install:
        - go get -d -t -v ./...
        - sh build-osx.sh
    - os: linux
      install:
        - go get -d -t -v ./...
        - sh build-linux.sh
    - os: windows
      install:
        - go get -d -t -v ./...
        - sh build-windows.bat
